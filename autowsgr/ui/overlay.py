"""游戏浮层 — 兼容层。

浮层检测与消除实现已迁移至 :mod:`autowsgr.ui.main_page.overlays`。
本模块保留 :class:`NetworkError` 定义，并对旧 API 做透明重导出。
"""

from __future__ import annotations


# ---------------------------------------------------------------------------
# 异常 (非主页面特有)
# ---------------------------------------------------------------------------


class NetworkError(Exception):
    """游戏进入断线/重连/登录界面。

    此异常由 UI 层检测并抛出，由上层 (GameOps/Scheduler) 捕获处理。
    UI 层本身不负责网络恢复逻辑。
    """
